---
"pyproject-udeps": patch
---

**fix**: add name-map aliases for `pyyaml`, `python-dateutil`, `pydocket`, `amplitude-analytics`, `opentelemetry-api`, and `opentelemetry-test-utils`, eliminating false positives on projects that import these packages.
