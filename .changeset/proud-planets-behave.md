---
"pyproject-udeps": patch
---

**fix**: the name map was audited against the wheel contents of the 1,500 most-downloaded PyPI packages, adding roughly 190 entries (`pillow` → `PIL`, `pyjwt` → `jwt`, `dnspython` → `dns`, `opencv-python` → `cv2`, and many more).
